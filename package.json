{
  "name": "oh-my-feed",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "refresh:feeds": "node scripts/fetch-feeds.mjs",
    "test": "node --test",
    "check": "node --check app.js && node --check src/domain.js && node --check scripts/fetch-feeds.mjs && node --test"
  }
}
