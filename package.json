{
  "name": "oh-my-feed",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm run build:metadata && wrangler dev",
    "refresh:feeds": "node scripts/fetch-feeds.mjs",
    "build:metadata": "node scripts/generate-build-metadata.mjs",
    "test": "npm run build:metadata && node --test",
    "check": "npm run build:metadata && node --check app.js && node --check src/domain.js && node --check src/worker.js && node --check scripts/fetch-feeds.mjs && node --check scripts/generate-build-metadata.mjs && node --test",
    "build": "npm run build:metadata && wrangler deploy --dry-run",
    "deploy:dry-run": "npm run build"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
